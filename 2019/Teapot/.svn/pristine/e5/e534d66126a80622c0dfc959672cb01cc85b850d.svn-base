package teapot;

import com.jogamp.opengl.GL;
import com.jogamp.opengl.GL2;
import com.jogamp.opengl.glu.GLU;
import com.jogamp.opengl.util.gl2.GLUT;

/**
 * ティーポットをレンダリング（描画）する。<br>
 * XYZ軸に加えてティーポットをソリッドとワイヤーフレームの双方で描画する。<br>
 * 視界（視点・注視点・上方向ベクトル・視界角・近・遠）もティーポットが保持する。<br>
 */
public class Teapot extends Object
{
	/**
	 * 視点を応答する。<br>
	 * @return 視点座標（x,y,zの配列）
	 */
	public double[] eyePoint()
	{
		return new double[] { 10.0d, 10.0d, 10.0d };
	}

	/**
	 * 注視点を応答する。<br>
	 * @return 注視点座標（x,y,zの配列）
	 */
	public double[] sightPoint()
	{
		return new double[] { 0.0d, 0.0d, 0.0d };
	}

	/**
	 * 上方向ベクトルを応答する。<br>
	 * @return 上方向ベクトル（x,y,zの配列）
	 */
	public double[] upVector()
	{
		return new double[] { 0.0d, 1.0d, 0.0d };
	}

	/**
	 * 視野角を応答する。<br>
	 * @return 視野角
	 */
	public double fovy()
	{
		return 15.0d;
	}

	/**
	 * 近を応答する。<br>
	 * @return 近
	 */
	public double near()
	{
		return 0.01d;
	}

	/**
	 * 遠を応答する。<br>
	 * @return 遠
	 */
	public double far()
	{
		return 100.0d;
	}

	/**
	 * レンダリング（描画）する。<br>
	 * @param gl グラフィックライブラリ
	 * @param glu グラフィックライブラリユーテリティ
	 * @param glut グラフィックライブラリユーテリティツールキット
	 */
	public void rendering(GL2 gl, GLU glu, GLUT glut)
	{
		// 作成してください。
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …
		// …

		return;
	}

	/**
	 * ウィンドウのタイトル（ラベル）を応答する。<br>
	 * @return タイトル文字列
	 */
	public String windowTitle()
	{
		return "Teapot";
	}
}
